# 배포 가이드

> 상태: **전체 배포 완료** — https://tongin-erp.vercel.app
> 최종 갱신: 2026-08-13

## 현재 구성

| 구성요소 | 상태 | 위치 |
| --- | --- | --- |
| 웹 (관리자 + 현장) | ✅ 배포됨 | Vercel `tongin-erp` — https://tongin-erp.vercel.app |
| DB (PostgreSQL) | ✅ 생성됨 | Neon `neon-bronze-elephant` (Vercel 마켓플레이스, 무료). 전체 마이그레이션 적용 완료 |
| API (NestJS) | ✅ 배포됨 | Render `tongin-erp-api` (무료, 싱가포르) — https://tongin-erp-api.onrender.com |
| 파일 저장 (작업 사진) | ⬜ 미구성 | S3 호환 환경변수만 준비 (`.env.example`) |

### 배포 중 겪은 것 (같은 실수를 피하려고 남긴다)

**1. 로컬 빌드만 성공하고 Render는 실패** — `prisma generate` 가 `DATABASE_URL`
을 못 찾아 P1012. 원인은 `.dockerignore` 부재였다. `COPY . .` 가 `apps/api/.env`
를 빌드 컨텍스트에 딸려 넣어 Prisma 가 거기서 값을 읽었고, git 에서 클론하는
Render 에는 그 파일이 없었다. **로컬 검증이 잘못된 이유로 통과하고 있었다.**
→ `.dockerignore` 추가. 패턴은 컨텍스트 루트 기준이라 `.env` 만으로는
`apps/api/.env` 가 안 걸리고 `**/.env` 가 필요하다.

**2. 런타임 `DATABASE_URL` 이 빈 값** — `render.yaml` 에 `sync: false` 로 선언한
`DATABASE_URL`·`ADMIN_PASSWORD` 가 Blueprint 생성 화면에서 입력받았는데도
서비스에 저장되지 않았다. → 두 값은 `render.yaml` 에서 빼고 대시보드
Environment 에서 직접 등록한다.

**3. `ADMIN_PASSWORD` 를 바꿔도 안 먹힘** — 시더는 admin 계정이 이미 있으면
건너뛴다. 비밀번호를 바꾸려면 계정을 지우고 재배포해야 한다.

웹은 `/api/*` 요청을 **같은 도메인**에서 프록시(`api/[...path].ts`)로 백엔드에 넘긴다.
이렇게 한 이유 — CORS 설정이 없어도 되고, 현장 PWA의 동일출처 캐시가 그대로 동작하며,
백엔드 호스트가 바뀌어도 Vercel 환경변수 `API_ORIGIN` 만 고치면 된다(재배포 불필요).

지금은 `API_ORIGIN` 이 비어 있어 `/api/*` 가 503과 안내 메시지를 돌려준다.

## 비용 — 두 가지 경로

### A. 무료로 시작 (권장: 지금 단계)

| 구성 | 서비스 | 무료 범위 | 한계 |
| --- | --- | --- | --- |
| 웹 | **Vercel Hobby** | 대역폭 100GB/월 | 상업적 사용은 Pro 필요(약관 확인 필요) |
| DB | **Neon** 또는 **Supabase** | 0.5GB 내외 | 통인 초기 데이터엔 충분, 레거시 마이그레이션엔 부족 |
| API | **Render Free** | 750시간/월 | **15분 미사용 시 잠들고 첫 요청이 느림** |
| 파일 | **Cloudflare R2** | 10GB + 전송량 무료 | S3 호환이라 코드 그대로 |

합계 **월 $0**. 시연·내부 테스트용으로 적합하다.
Render 무료의 콜드스타트가 현장에서는 거슬리므로, 실사용 직전에 유료(월 $7)로 올리거나 아래 B로 간다.

### B. AWS 서울 (설계노트 확정 구성 — 실운영)

소규모 상시가동 기준 **월 $40~60** 수준. 항목별로:

| 항목 | 구성 | 월 비용(대략) |
| --- | --- | --- |
| API | ECS Fargate 0.25vCPU / 0.5GB 상시 | $9 |
| DB | RDS `db.t4g.micro` Single-AZ, 20GB | $13~15 |
| 로드밸런서 | ALB | **$18~20** |
| 파일 | S3 + CloudFront (소량) | $1~3 |
| NAT Gateway | 프라이빗 서브넷에서 아웃바운드 필요 시 | **+$35** |

주의할 점 두 가지:

- **ALB가 컴퓨팅보다 비싸다.** 트래픽이 적어도 고정비다. 단일 컨테이너면 App Runner나
  Fargate + 퍼블릭 IP로 ALB를 빼는 편이 싸다.
- **NAT Gateway를 무심코 켜면 가장 큰 항목이 된다.** 퍼블릭 서브넷 + 보안그룹으로 시작할 것.

**프리티어 주의** — AWS 프리티어 정책은 2025년 7월 이후 신규 계정에 크레딧 방식으로
바뀌었다. 계정 생성 시점에 따라 적용 내용이 다르므로 콘솔에서 직접 확인이 필요하다.
어느 쪽이든 12개월 뒤에는 위 금액이 그대로 청구된다.

### 데이터가 늘면 비용이 어떻게 되나

행 수 자체보다 **상시 가동되는 인스턴스 시간**이 대부분을 차지한다. 통인 규모(연 수만 건)의
행 데이터는 20GB 안에 들어가고, 스토리지는 GB당 월 $0.1 수준이라 영향이 작다.
비용이 뛰는 시점은 인스턴스 크기를 올리거나 Multi-AZ(이중화)를 켤 때다.

## 배포 절차

### 1. 웹 (완료)

```bash
vercel deploy --prod
```

저장소 루트의 `vercel.json` 이 pnpm 워크스페이스를 빌드해 `apps/web/dist` 를 배포한다.

### 2. DB

Neon/Supabase/RDS 중 하나에서 PostgreSQL 16 인스턴스를 만들고 접속 문자열을 받는다.
마이그레이션은 API 컨테이너가 시작할 때 `prisma migrate deploy` 로 자동 적용된다.

### 3. API (Render)

저장소 루트의 `render.yaml` 이 Blueprint다. Render 대시보드에서
**New → Blueprint → 이 저장소 선택** 하면 `tongin-erp-api` 서비스가 자동 생성된다.

> 같은 Render 계정에 **이 저장소와 무관한 예전 `tongin-api` 서비스**가 있다(3개월 전 생성,
> Node 런타임). 이 저장소는 2026-06-22에 시작됐으므로 그보다 앞선 다른 프로젝트다.
> 이름 충돌을 피하려고 새 서비스는 `tongin-erp-api` 로 만든다.

대시보드에서 직접 넣어야 하는 값 두 개(저장소에 남기지 않기 위해 `sync: false`):

| 변수 | 값 |
| --- | --- |
| `DATABASE_URL` | Neon **직접 연결** 문자열 (`DATABASE_URL_UNPOOLED`) |
| `ADMIN_PASSWORD` | 직접 정한 강한 비밀번호 |

`JWT_SECRET` 은 Render가 자동 생성하고, `WEB_APP_URL` 은 `render.yaml` 에 이미 있다.

> **풀러가 아니라 직접 연결을 쓰는 이유** — 컨테이너 시작 시 `prisma migrate deploy` 가
> 도는데, 마이그레이션은 PgBouncer 같은 커넥션 풀러를 통과하지 못한다.
> 상시 가동 컨테이너 하나라서 풀링의 이점도 없다.

로컬에서 이미지를 확인하려면:

```bash
docker build -f apps/api/Dockerfile -t tongin-api .
```

필수 환경변수:

| 변수 | 설명 |
| --- | --- |
| `DATABASE_URL` | PostgreSQL 접속 문자열 |
| `JWT_SECRET` | **반드시 새로 생성** (`openssl rand -base64 48`) |
| `ADMIN_PASSWORD` | 초기 관리자 비밀번호. **설정하지 않으면 `admin1234` 가 된다** |
| `WEB_APP_URL` | `https://tongin-erp.vercel.app` — 알림톡 링크·구글 OAuth 리디렉션에 쓰임 |
| `PORT` | 플랫폼이 주입하면 그대로 |

선택: `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GOOGLE_REDIRECT_URI` (캘린더 연동),
`CORS_ORIGINS` (같은 도메인 프록시를 쓰면 불필요).

### 4. 웹 ↔ API 연결

Vercel 프로젝트 환경변수에 백엔드 주소를 넣고 재배포한다.

```bash
vercel env add API_ORIGIN production   # 예: https://tongin-api.onrender.com
vercel deploy --prod
```

### 5. 파일 저장 (작업 사진 — 아직 코드 미구현)

S3 또는 R2 버킷을 만들고 `.env.example` 의 `STORAGE_*` 를 채운다.
업로드는 **프리사인 URL로 브라우저에서 직접** 올리는 방식을 쓸 예정이다 —
Vercel 프록시의 본문 크기 제한(4.5MB)을 피하고, 현장 사진이 API 서버를 거치지 않게 하기 위함.

## AWS 이전 (나중에 실납품할 때)

지금 구성은 벤더 전용 기능을 쓰지 않아 이전이 어렵지 않다. **실제로 검증했다** —
Neon(PG17)에서 덤프를 떠서 사용자·DB명·버전이 모두 다른 Postgres(PG16)에 복원한 뒤,
같은 API 이미지를 붙여 로그인까지 되는 것을 확인했다. 43개 테이블·데이터가 모두 일치했고
추가 마이그레이션도 필요 없었다.

| 구성요소 | 이전 방법 | 난이도 |
| --- | --- | --- |
| DB | `scripts/db-dump.sh` → `scripts/db-restore.sh` | 명령 두 줄 |
| API | **같은 Dockerfile** 을 ECS/App Runner에 | 설정만 |
| 웹 | 정적 빌드(`apps/web/dist`)를 S3 + CloudFront에 | 파일 복사 |
| 파일 | S3 호환으로만 접근하면 엔드포인트 교체 | 코드 변경 없음 |

```bash
# 1. 현재 DB 덤프 (pg_dump 버전은 Docker로 고정되므로 로컬 설치 불필요)
./scripts/db-dump.sh "$NEON_URL"

# 2. RDS로 복원
./scripts/db-restore.sh backup/tongin-....dump "$RDS_URL"
```

### 이전 시 실제로 신경 쓸 것

코드가 아니라 **주변 설정**이다.

- **다운타임** — 덤프~복원 사이에 들어온 데이터는 유실된다. 서비스를 잠깐 내리고
  옮기거나, 무중단이 필요하면 논리적 복제를 써야 한다
- **구글 OAuth 리디렉션 URI** — 도메인이 바뀌면 구글 콘솔에 새 URI를 등록해야 한다
- **`WEB_APP_URL`** — 알림톡 링크가 이 값으로 나가므로 반드시 갱신
- **파일 저장** — Vercel Blob 같은 자체 SDK를 쓰면 이 부분만 다시 짜야 한다.
  그래서 `.env.example` 을 S3 호환(`STORAGE_ENDPOINT`) 방식으로 잡아 뒀다

## 신규 DB 첫 사용 시

시더는 권한·역할·관리자 계정만 만든다. **조직(지점)은 만들지 않는다.**
접수·견적은 담당 지점이 있어야 등록되므로, 배포 후 관리자로 로그인해
**조직관리에서 지점을 먼저 등록**해야 한다.

## 배포 전 반드시 확인할 것

- [ ] **`ADMIN_PASSWORD` 를 설정했는가** — 안 하면 공개 URL에 `admin` / `admin1234` 가 열린다
- [ ] **`JWT_SECRET` 을 새로 생성했는가** — `.env.example` 값이나 개발용 값을 쓰면 토큰 위조가 가능하다
- [ ] 구글 OAuth를 쓴다면 승인된 리디렉션 URI에 운영 도메인을 등록했는가
- [ ] 알림톡 발송이 아직 스텁(`StubNotificationProvider`)이라는 점 — 실제 발송은 별도 연동 필요
- [ ] 결제(가상계좌)도 스텁(`StubPaymentProvider`) — 토스페이먼츠 연동 전까지는 수동 확인

## 알아 둘 제약

- **알림톡·결제·전자서명은 모두 스텁이다.** 로그만 남기고 성공 처리한다. 실제 연동 전에는
  운영에 쓸 수 없다.
- **현장 PWA의 쓰기(작업 시작·완료)는 오프라인 큐잉을 하지 않는다.** 온라인에서만 된다.
- **레거시 마이그레이션(MIG-01)은 미착수다.** 무료 DB 용량으로는 부족할 가능성이 높다.
