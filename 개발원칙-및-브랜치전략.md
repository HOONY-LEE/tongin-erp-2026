# 개발 원칙 & 브랜치 전략

> 코드 작성 전 필독. 특히 **여러 세션이 동시에 일할 때 충돌을 막는 규칙**.
> 최종 수정: 2026-06-22

---

## 1. 멀티 세션 충돌 방지 (가장 중요)

여러 세션/사람이 동시에 작업해도 안전하도록:

1. **모든 작업은 기능 항목(ID)에서 출발.** [기능목록-및-로드맵.md](기능목록-및-로드맵.md)에 없는 일은 먼저 항목으로 추가.
2. **작업 시작 = "진행중 현황" 등록.** 기능목록 문서 상단 표에 `기능ID / 브랜치명 / 담당(세션) / 시작일`을 적고 시작. 끝나면 제거 + 상태 ✅.
3. **1 기능 = 1 브랜치 = 1 PR.** 한 브랜치에서 여러 기능 섞지 말 것.
4. **모듈 경계 안에서 작업.** 모듈러 모놀리식이므로 `modules/<도메인>/` 폴더 안에서 작업하면 충돌 거의 없음. 공유 영역(`packages/shared`, 공통코드, 마스터 스키마)은 변경 시 PR에 명시 + 작게.
5. **시작 전 항상 `main`에서 최신 pull.** 작업 중 main이 바뀌면 자주 rebase.
6. **큰 변경은 쪼갠다.** 리뷰 가능한 크기(가능하면 400줄 이하)로.

---

## 2. 브랜치 전략 — GitHub Flow (단순)

- `main`은 **항상 배포 가능** 상태 유지.
- 기능 브랜치를 main에서 분기 → PR → 리뷰 → **squash merge** → main.
- 장수(long-lived) `develop` 브랜치 없음 (Git Flow는 이 규모에 과함).

### 브랜치 네이밍
```
feat/<기능ID>-<짧은설명>     예) feat/EST-01-cbm-estimate
fix/<기능ID>-<짧은설명>
chore/<설명>  docs/<설명>  refactor/<설명>
```

### 릴리스
- main 머지 → staging 자동 배포 → 검증 → prod 태그 배포.
- 태그: `v<major>.<minor>.<patch>`.

---

## 3. 커밋 규칙 — Conventional Commits

```
<type>(<scope>): <설명>

feat(estimate): CBM 자동합산 추가
fix(payment): 가상계좌 웹훅 중복처리 방지
```
type: feat / fix / docs / refactor / test / chore / perf

---

## 4. 코드 품질

- **TypeScript strict 모드** (또는 Spring이면 정적분석 + 코틀린 null-safety).
- ESLint + Prettier (자동 포맷). 커밋 훅으로 강제.
- **테스트**: 도메인 핵심 로직(가격산정·물량합산·정산·상태전이)은 단위 테스트 필수. 통합 테스트는 핵심 플로우(접수→완료).
- PR 머지 조건: CI 통과(빌드+린트+테스트) + 리뷰 1.

---

## 5. 설계 원칙 (코드에 반영)

- **데이터 모델 우선** (화면 먼저 X). 설계노트 1-A.
- **마스터/전표 분리**, **전표 불변 + 감사로그**.
- **권한은 데이터** (역할×기능×데이터범위×조직), 코드 하드코딩 금지.
- **문서는 데이터에서 동적 생성**, 고정 양식 금지.
- **외부 연동은 추상화 레이어 뒤** (벤더 교체 가능: OCR·결제·알림톡·전자계약).
- **이벤트로 사이드이펙트 분리** (알림·전표·상태전환).

---

## 6. 문서 갱신 규칙

- 도메인 결정이 바뀌면 → **설계노트** 갱신.
- 기능 진행/완료 → **기능목록** 상태 갱신 (작업의 일부).
- 스택/구조 결정 → **기술스택** 문서 + 확정 표시.
- 큰 마일스톤 → README 상태 스냅샷 갱신.

---

## 7. 보안·개인정보 (한국)

- 고객 개인정보(이름·주소·연락처·계약서) = 암호화 저장 + 접근 최소권한 + 감사로그.
- 개인정보 처리 문서/PDF·OCR 결과는 **국내 거주** 원칙.
- 비밀키·토큰은 시크릿 매니저 (레포에 절대 커밋 금지).
