# 기술 스택 & 아키텍처

> 상태: **핵심 스택 확정 (2026-06-22)**. 백엔드·클라우드·모바일·DB 결정 완료.
> 최종 수정: 2026-06-22

---

## 0. 결정 요약 (한눈에)

| 영역 | 추천 | 비고 |
|------|------|------|
| 저장소 구조 | ✅ 모노레포 (pnpm + Turborepo) | 백엔드·웹·모바일·공유타입 한 곳 |
| 백엔드 | ✅ **TypeScript + NestJS** | 프론트와 한 언어, AI/연동 친화 |
| DB | ✅ **PostgreSQL** | MSSQL 대체, JSONB(유연속성), 단일 진실원 |
| 웹(관리자·현장·가맹점) | ✅ React + TypeScript + **Ant Design**, **반응형(태블릿/모바일 커버)** | ERP 표·폼 밀도 + 태블릿 day-1 |
| 모바일 | ✅ **반응형 웹 우선 → 네이티브(React Native) 추가** | §4. 폰 현장기능(카메라·오프라인·푸시)은 네이티브 |
| 이벤트 | ✅ 인프로세스 이벤트 + 트랜잭션 outbox → 후에 브로커 | "가볍게 시작" |
| 클라우드 | ✅ **AWS 서울리전(ap-northeast-2)** | 개인정보 국내 거주 §5 |
| 인증/권한 | ✅ 인앱 RBAC + JWT | 우리 권한모델(역할×기능×데이터범위×조직) |
| CI/CD | ✅ GitHub Actions | |

---

## 1. 아키텍처 원칙

- **API-first 모듈러 모놀리식**: 처음엔 하나의 배포 가능한 백엔드(모듈 경계 명확) → 필요 시 서비스 분리. MSA로 시작하지 않음(소규모 팀에 과함).
- **도메인 주도(DDD-lite)**: 모듈 = 도메인 경계(접수/견적/계약/작업/자재/정산/권한…). 설계노트의 모듈 맵과 1:1.
- **이벤트 기반**: 도메인 이벤트 + outbox로 사이드이펙트(알림톡·상태전환·매출전표) 분리. AI hook의 전제 (설계노트 1-A).
- **전표 원칙 = 감사로그**: 모든 거래는 불변 + 변경이력. 서명 계약서는 동결(설계노트 E-0).
- **멀티테넌시**: 단일 DB + `조직단위(tenant)` 행 단위 격리. 모든 전표에 조직 귀속.
- **문서 = 데이터에서 동적 생성**: 고정 양식 폐기, 입력된 라인만 렌더(설계노트 F-1).

---

## 2. 백엔드 선택 ✅ (확정: NestJS)

| | TypeScript + NestJS ⭐ | Kotlin/Java + Spring Boot |
|---|---|---|
| 장점 | 프론트와 **한 언어**(공유 타입), 빠른 속도, AI/연동 SDK 풍부, 소규모 팀 효율 | 한국 엔터프라이즈 표준, **개발자 채용풀 큼**, ERP 안정성 검증 |
| 단점 | 대규모 채용풀은 Java보다 작음 | 프론트와 언어 분리, 초기 보일러플레이트 |
| 추천 상황 | 모던·소규모·빠른 팀 | Java 인력 위주로 채용/확장 |

→ **권장: NestJS** (한 언어로 풀스택, AI·연동 친화). 단, **Java 개발자 위주로 팀을 꾸릴 계획이면 Spring Boot**가 더 안전.

---

## 3. 데이터 계층

- **PostgreSQL** 단일 DB. ORM: Prisma(NestJS) 또는 TypeORM. (Spring이면 JPA)
- 유연 속성(품목 가변속성 등) = **JSONB 컬럼** (스키마 변경 없이 확장, 설계노트 1-A).
- 마이그레이션 도구로 스키마 버전관리(코드로 관리).

---

## 4. 프론트엔드

- **웹(관리자·현장·가맹점)**: React + TS + Ant Design(표·폼·트리 풍부) — 레거시 고밀도 화면 대체. **반응형으로 설계 → 태블릿/모바일 브라우저 day-1 커버.**
- **모바일 전략 (단계적)**:
  - **Phase 1**: 반응형 웹 우선. 태블릿은 이걸로 충분. 폰도 브라우저로 사용 가능.
  - **Phase 3**: **네이티브 앱(React Native, iOS+Android)** 추가 — 카메라(계약서 스캔→OCR), 오프라인(신호 약한 현장), 푸시. 특히 iOS는 PWA로 한계라 네이티브 필요.
  - ⚠️ **재작성 방지**: 비즈니스 로직·API 클라이언트·디자인 시스템을 `packages/shared`로 공유 → 네이티브 전환 시 재사용(RN-for-Web으로 컴포넌트까지 공유 가능).
- **공유 타입**: 모노레포 `packages/shared`에 도메인 타입·API 계약 공유(프론트-백 불일치 방지).

---

## 5. 외부 연동 (설계노트 11장)

| 용도 | 서비스 |
|------|--------|
| 계약금 결제·가상계좌·웹훅 | 토스페이먼츠 |
| 알림톡/SMS | 솔라피·알리고 등(카카오 알림톡 대행) |
| 전자계약 | 모두싸인 / 이폼사인 |
| OCR(종이계약) | 네이버 CLOVA OCR (국내DC, 추상화 레이어 뒤) |
| 지도·거리계산 | 카카오맵 / 네이버맵 (한국 주소·지오코딩) |
| 회계·급여 | 더존 등 연동 |
| BI | Metabase/Superset(같은 DB 연결) |

---

## 6. 인프라/배포 ✅ (확정: AWS 서울)

- **클라우드**: ✅ **AWS 서울리전(ap-northeast-2)** — 성숙한 생태계 + 국내 거주.
  - (참고) CLOVA OCR은 네이버클라우드지만 외부 API로 호출하면 되므로 AWS와 무관하게 사용 가능.
- 컨테이너: Docker. 오케스트레이션은 소규모면 관리형(ECS/Fargate 등)으로 단순하게 시작.
- 환경: dev / staging / prod 분리.

---

## 7. 미확정/추후 (TBD)

- [x] ~~백엔드 언어~~ → NestJS 확정
- [x] ~~클라우드 벤더~~ → AWS 서울 확정
- [x] ~~모바일 방식~~ → 반응형 웹 우선 → 네이티브(RN) 추가 확정
- [ ] 회계 SW 연동 대상 (더존 등)
- [ ] 알림톡/전자계약/지도 벤더 세부 선정
- [ ] ORM 선택 (Prisma vs TypeORM)
